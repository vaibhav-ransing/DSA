package First10;

public class Crossword {
    // Too much code loop in each word try to place it vertically and horizontally and if fits then place it and place next word starting from 0,0 

    public boolean placeWordInCrossword(char[][] board, String word) {
        
        return true;
    }
    
}
