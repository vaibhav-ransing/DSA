package Revision;

import java.util.Arrays;

public class MaxTastinessOfCandybasket {

    public int maximumTastiness(int[] arr, int k) {
        // 1, 2, 5, 8, 13, 21
        Arrays.sort(arr);
        int left = 0, right = 9999;

        while (left <= right) {
            int mid = left + (right - left) / 2;

        }
    }

    public boolean canPickKElements(int[] arr, int diff) {
        
    }
}
